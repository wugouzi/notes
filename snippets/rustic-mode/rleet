# -*- mode: snippet -*-
# name: rleet
# key: rleet
# --
impl Solution {
    pub fn ${1:function}(${2:input}) -> ${3:output} {
        $0
    }
}

pub struct Solution;

#[cfg(test)]
mod tests {
    extern crate test;
    use std::env;
    use super::*;

    #[test]
    fn test() {
        env::set_var("RUST_BACKTRACE", "1");
        assert_eq!(Solution::$1(), );
    }

}